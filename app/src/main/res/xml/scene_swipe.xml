<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ConstraintSet android:id="@+id/rest">

        <Constraint
            android:id="@id/topCard"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="@dimen/image_item_padding"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />
    </ConstraintSet>

    <ConstraintSet
        android:id="@+id/pass"
        app:deriveConstraintsFrom="@+id/rest">

        <Constraint
            android:id="@id/topCard"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginStart="@dimen/image_item_padding"
            android:layout_marginTop="@dimen/image_item_padding_top"
            android:layout_marginEnd="@dimen/image_item_padding_max"
            android:layout_marginBottom="@dimen/image_item_padding_bottom"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintWidth_percent="0.7" />
    </ConstraintSet>

    <ConstraintSet
        android:id="@+id/like"
        app:deriveConstraintsFrom="@id/rest">

        <Constraint
            android:id="@id/topCard"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginStart="@dimen/image_item_padding_max"
            android:layout_marginTop="@dimen/image_item_padding_top"
            android:layout_marginEnd="@dimen/image_item_padding"
            android:layout_marginBottom="@dimen/image_item_padding_bottom"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintWidth_percent="0.7" />
    </ConstraintSet>


    <Transition
        app:constraintSetEnd="@+id/pass"
        app:constraintSetStart="@+id/rest"
        app:duration="300">

        <OnSwipe
            app:dragDirection="dragLeft"
            app:onTouchUp="autoComplete"
            app:touchAnchorId="@id/topCard"
            app:touchAnchorSide="left"
            app:touchRegionId="@id/topCard" />
    </Transition>

    <Transition
        app:constraintSetEnd="@+id/like"
        app:constraintSetStart="@+id/rest"
        app:duration="300">

        <OnSwipe
            app:dragDirection="dragRight"
            app:onTouchUp="autoComplete"
            app:touchAnchorId="@+id/topCard"
            app:touchAnchorSide="right"
            app:touchRegionId="@id/topCard" />
    </Transition>

</MotionScene>